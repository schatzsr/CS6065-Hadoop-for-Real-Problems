#!/usr/bin/env python

import sys
import string
import json

example_text = 'example'

for line in sys.stdin:
    tweet_data = json.loads(line)
    print 'ExampleKey\t%s' % example_text

if not character_count <= 1:
    print 'SentenceChar\t%s' % character_count
